# Frontend production settings for the same-domain Caddy deployment.
#
# VITE_API_URL is intentionally relative so the browser calls the same origin
# that served the page. Caddy forwards /api/* to the local FabOS API, so no
# cross-origin requests are made and no CORS configuration is required.
VITE_API_URL=/api
