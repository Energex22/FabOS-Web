# FabVex production reverse proxy template.
# Replace YOUR-DOMAIN before use.
#
# Caddy terminates HTTPS automatically and proxies only /api traffic to FabOS.
# Keep FabOS itself bound to 127.0.0.1:8000.

YOUR-DOMAIN {
    encode gzip

    root * C:\FabOS\FabOS-Web\dist

    handle /api/* {
        reverse_proxy 127.0.0.1:8000
    }

    handle {
        try_files {path} {path}/ /index.html
        file_server
    }

    header {
        X-Content-Type-Options "nosniff"
        Referrer-Policy "strict-origin-when-cross-origin"
        X-Frame-Options "SAMEORIGIN"
        Permissions-Policy "camera=(), microphone=(), geolocation=()"
    }
}
